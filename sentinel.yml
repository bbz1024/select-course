version: "v1"
sentinel:
  app:
    name: select-course
    type: 0
  log:
    dir: "./logs/csp"
    pid: false
    metric:
      maxFileCount: 14
      flushIntervalSec: 1
  stat:
    system:
      collectIntervalMs: 1000